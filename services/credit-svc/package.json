{
  "name": "@neolend/credit-svc",
  "version": "0.1.0",
  "private": true,
  "description": "Servicio 3 — Decisión + Crédito (CQRS+Event Sourcing) + Desembolso + Ledger (incisos III, IV).",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "start:dev": "ts-node-dev --respawn src/main.ts",
    "lint": "eslint \"src/**/*.ts\"",
    "test": "echo \"(sin tests aún)\" && exit 0"
  },
  "dependencies": {
    "@nestjs/common": "^10.3.0",
    "@nestjs/core": "^10.3.0",
    "@nestjs/platform-express": "^10.3.0",
    "@nestjs/typeorm": "^10.0.2",
    "typeorm": "^0.3.20",
    "@neolend/ts-common": "workspace:*",
    "@neolend/ts-events": "workspace:*",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.1",
    "pg": "^8.11.3",
    "reflect-metadata": "^0.2.1",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.0"
  }
}
