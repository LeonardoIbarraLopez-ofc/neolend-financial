# ============================================================
#  NeoLend Financial — Entorno LOCAL completo (gratuito)
#  Arranque:  docker compose up -d
#  Requiere:  Docker Desktop. Sin cuentas ni API keys de pago.
# ============================================================
name: neolend

services:
  # ---------- Infraestructura ----------
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-neolend}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-neolend_local_pw}
      POSTGRES_DB: postgres
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./infra/postgres/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    healthcheck:
      # -d postgres: sondear la base de mantenimiento existente evita el ruido
      # "FATAL: database \"neolend\" does not exist" (pg_isready usa el user como db por defecto).
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER:-neolend} -d postgres']
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    ports:
      - '6379:6379'
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 5s
      timeout: 3s
      retries: 10

  # Redpanda = bus de eventos compatible con la API de Kafka (liviano para local)
  redpanda:
    image: redpandadata/redpanda:v24.1.1
    command:
      - redpanda
      - start
      - --smp=1
      - --memory=512M
      - --overprovisioned
      - --node-id=0
      - --kafka-addr=PLAINTEXT://0.0.0.0:9092
      - --advertise-kafka-addr=PLAINTEXT://redpanda:9092
    ports:
      - '9092:9092'
    healthcheck:
      test: ['CMD-SHELL', 'rpk cluster health | grep -q "Healthy:.*true"']
      interval: 10s
      timeout: 5s
      retries: 10

  # Crea los topics una vez (evita el "not leader" del auto-create en el primer produce)
  redpanda-init:
    image: redpandadata/redpanda:v24.1.1
    depends_on:
      redpanda: { condition: service_healthy }
    entrypoint: ['/bin/sh', '-c']
    command:
      - >
        rpk topic create origination.events scoring.events credit.events
        disbursement.events collections.events gamification.events
        --brokers redpanda:9092 -r 1 -p 1 || true
    restart: 'no'

  # ---------- Mocks locales (sustituyen proveedores externos) ----------
  bureau-soap-mock:
    build:
      context: .
      dockerfile: ./mocks/bureau-soap-mock/Dockerfile
    ports:
      - '9101:9101'

  wallet-mock:
    build:
      context: .
      dockerfile: ./mocks/wallet-mock/Dockerfile
    ports:
      - '9102:9102'

  whatsapp-mock:
    build:
      context: .
      dockerfile: ./mocks/whatsapp-mock/Dockerfile
    ports:
      - '9103:9103'

  # ---------- Microservicios (1..6) ----------
  gateway-origination-svc:
    build:
      context: .
      dockerfile: ./services/gateway-origination-svc/Dockerfile
    env_file: .env
    environment:
      DATABASE_URL: postgres://gateway_origination:svc_pw@postgres:5432/origination_db
      SCORING_URL: http://scoring-svc:8102
      CREDIT_URL: http://credit-svc:8103
    # El "gateway" (borde) es el módulo de este servicio; escucha en 8101.
    ports:
      - '8101:8101'
    depends_on:
      postgres: { condition: service_healthy }
      redpanda: { condition: service_healthy }
      redpanda-init: { condition: service_completed_successfully }

  scoring-svc:
    build:
      context: .
      dockerfile: ./services/scoring-svc/Dockerfile
    env_file: .env
    ports:
      - '8102:8102'
    depends_on:
      redis: { condition: service_healthy }
      redpanda: { condition: service_healthy }

  credit-svc:
    build:
      context: .
      dockerfile: ./services/credit-svc/Dockerfile
    env_file: .env
    environment:
      DATABASE_URL: postgres://credit:svc_pw@postgres:5432/credit_db
    ports:
      - '8103:8103'
    depends_on:
      postgres: { condition: service_healthy }
      redpanda: { condition: service_healthy }

  servicing-svc:
    build:
      context: .
      dockerfile: ./services/servicing-svc/Dockerfile
    env_file: .env
    environment:
      DATABASE_URL: postgres://servicing:svc_pw@postgres:5432/servicing_db
    ports:
      - '8104:8104'
    depends_on:
      postgres: { condition: service_healthy }
      redpanda: { condition: service_healthy }

  investor-svc:
    build:
      context: .
      dockerfile: ./services/investor-svc/Dockerfile
    env_file: .env
    environment:
      DATABASE_URL: postgres://investor:svc_pw@postgres:5432/investor_db
    ports:
      - '8105:8105'
    depends_on:
      postgres: { condition: service_healthy }
      redpanda: { condition: service_healthy }

  compliance-svc:
    build:
      context: .
      dockerfile: ./services/compliance-svc/Dockerfile
    env_file: .env
    environment:
      DATABASE_URL: postgres://compliance:svc_pw@postgres:5432/compliance_db
    volumes:
      # Lectura-escritura: compliance-svc autogenera el par RSA de firma si no existe.
      - ./keys:/keys
      - ./worm:/worm
    ports:
      - '8106:8106'
    depends_on:
      postgres: { condition: service_healthy }
      redpanda: { condition: service_healthy }

  # ---------- Frontend ----------
  web-portal:
    build:
      context: .
      dockerfile: ./clients/web-portal/Dockerfile
    ports:
      - '5173:5173'
    environment:
      VITE_GATEWAY_URL: http://gateway-origination-svc:8101
    depends_on:
      - gateway-origination-svc

# ---------- Volúmenes persistentes ----------
volumes:
  pgdata: